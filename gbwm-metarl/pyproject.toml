[project]
name = "gbwm-metarl"
version = "0.1.0"
description = "NumPy-first reproduction scaffold for MetaRL goals-based wealth management."
requires-python = ">=3.11"
dependencies = [
  "numpy",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

